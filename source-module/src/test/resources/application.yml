 firehose:
    doppler-url: ws://doppler.10.1.1.1.xip.io
    doppler-events: "HttpStartStop"
    #only needed if you are deploying to CF
    authentication-url:
    cf-domain:
    username:
    password:
    trust-self-certs: true

 security:
    user:
       password: s3cr3t

 logging:
     level:
        org.springframework.cloud: "DEBUG"
        org.springframework.xd.dirt.integration.rabbit: "DEBUG"
        org.springframework.amqp: "DEBUG"